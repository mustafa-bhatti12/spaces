# Bind address for this static site + its /connect proxy.
PORT=8888

# Where token-service is running. This process calls it server-side so the
# shared secret below never reaches the browser.
TOKEN_SERVICE_URL=http://localhost:8880
TOKEN_SERVICE_SHARED_SECRET=local-dev-secret-not-for-production
