{
  "name": "test-call",
  "version": "0.1.0",
  "private": true,
  "description": "Throwaway static site for manually testing the LiveKit media server between two machines. Not a product, not deployed anywhere — delete once the real Petition Studio UI has been click-tested.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@fastify/static": "^10.0.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.12.1",
    "http-proxy": "^1.18.1"
  }
}
