{
  "name": "test-call",
  "version": "0.1.0",
  "private": true,
  "description": "Throwaway call UI for testing the LiveKit stack. Deployed on Railway as a stand-in for Petition Studio (same token-service + public LiveKit path) — delete once the real Petition Studio UI has been click-tested.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@fastify/static": "^10.0.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.12.1",
    "http-proxy": "^1.18.1"
  }
}
