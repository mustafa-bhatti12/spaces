{
  "name": "space-admin",
  "version": "0.1.0",
  "private": true,
  "description": "Operator control center for Space: live rooms, moderation, recordings, service health. Talks to token-service's /admin routes.",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "fastify": "^5.12.1"
  }
}
